// everything related to the dialog goes here:


const initializeDialog = () => {
    const welcomeDialog = document.querySelector("#welcomeDialog")

    welcomeDialog.showModal();
}

export { initializeDialog }